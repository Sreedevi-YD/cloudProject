package com.enterprise.onboarding.domain.model;

/** Department a task is assigned to for execution (distinct from an employee's own department). */
public enum OwningDepartment {
    HR,
    IT,
    MANAGER,
    EMPLOYEE
}
